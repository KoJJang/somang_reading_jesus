# 리딩 지저스 일정 설정 가이드

## 일정 지연/변경 관리

리딩 지저스 일정에 휴식 주가 생기거나 변경될 때 손쉽게 대응할 수 있도록 중앙 설정 시스템을 제공합니다.

## 설정 파일 위치

```
lib/config/schedule_config.dart
```

## 설정 방법

### 1. 휴식 주 설정 (breakWeeks)

`breakWeeks` 리스트에 휴식 주의 시작일을 추가합니다.

```dart
class ScheduleConfig {
  static final List<DateTime> breakWeeks = [
    DateTime(2025, 9, 29),  // 1차 휴식 주 (9/29 ~ 10/4)
    DateTime(2025, 10, 6),  // 2차 휴식 주 (10/6 ~ 10/11)
  ];
  ...
}
```

### 2. 휴식 주 동작 원리

- 각 날짜는 해당 주의 **월요일**을 기준으로 자동 계산됩니다
- 월요일부터 토요일(6일)이 휴식 주로 처리됩니다
- 일요일은 원래 쉬는 날이므로 별도 처리 불필요
- 휴식 주에는 읽기 일정이 표시되지 않습니다
- 캘린더에서 **보라색**으로 표시됩니다

### 3. 실제 사용 예시

#### 예시 1: 2주 휴식 추가

```dart
static final List<DateTime> breakWeeks = [
  DateTime(2025, 9, 29),  // 1차 휴식 (9/29~10/4)
  DateTime(2025, 10, 6),  // 2차 휴식 (10/6~10/11)
];
```

**결과:**
- 9월 29일 ~ 10월 4일: 휴식 주 (보라색)
- 10월 6일 ~ 10월 11일: 휴식 주 (보라색)
- 10월 15일: 10월 1일의 일정이 표시됨 (2주 분량 자동 보정)

#### 예시 2: 휴식 주 없음

```dart
static final List<DateTime> breakWeeks = [];
```

**결과:**
- 모든 날짜가 원래 일정대로 진행
- 10월 15일: 10월 15일의 일정 표시

## 적용되는 기능

일정 오프셋은 다음 기능들에 자동으로 적용됩니다:

1. **오늘의 말씀 (홈 화면)**
   - 오늘의 읽기 본문 표시
   - 유튜브 강의 링크

2. **일별 해설 이미지**
   - 일별 해설 이미지 URL 생성
   - 로컬/원격 이미지 경로

3. **주간 해설**
   - 해당 주차의 주간 해설 매칭

4. **캘린더 화면**
   - 특정 날짜의 일정 조회

## 주의사항

1. **앱 재빌드 필요**: 설정 변경 후 앱을 다시 빌드해야 적용됩니다.

2. **CSV 파일과의 관계**: 
   - `assets/data/url_list.csv`에는 원본 날짜가 저장되어 있습니다.
   - `delayDays` 설정으로 날짜를 조정하여 CSV에서 데이터를 찾습니다.
   - CSV 파일 자체를 수정할 필요는 없습니다.

3. **일관성 유지**: 
   - 모든 사용자가 같은 일정을 보려면 동일한 `delayDays` 값을 사용해야 합니다.
   - 앱 배포 시 올바른 값으로 설정했는지 확인하세요.

## 변경 이력

- 2025-10-15: 초기 오프셋 시스템 구현 (2주 지연 대응)

