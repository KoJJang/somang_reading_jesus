# 리딩 지저스 일정 설정 가이드

## 일정 지연/변경 관리

리딩 지저스 일정이 지연되거나 변경될 때 손쉽게 대응할 수 있도록 중앙 설정 시스템을 제공합니다.

## 설정 파일 위치

```
lib/config/schedule_config.dart
```

## 설정 방법

### 1. 일정 지연 설정 (delayDays)

`delayDays` 값을 수정하여 일정 지연을 반영할 수 있습니다.

```dart
class ScheduleConfig {
  static const int delayDays = 14;  // 이 값을 수정
  ...
}
```

### 2. 설정 값의 의미

- **양수 값**: 일정이 지연된 경우
  - 예: `delayDays = 14` → 2주 지연
  - 예: `delayDays = 7` → 1주 지연
  
- **0**: 원래 일정대로 진행
  - 예: `delayDays = 0` → 지연 없음

- **음수 값**: 일정이 앞당겨진 경우 (거의 사용 안 함)
  - 예: `delayDays = -7` → 1주 앞당김

### 3. 실제 사용 예시

#### 예시 1: 2주 지연된 경우

```dart
static const int delayDays = 14;
```

- 현재 날짜: 2025년 10월 15일
- 앱에서 표시되는 일정: 2025년 10월 1일의 일정
- 결과: 사도행전 21-22장 (2주 전 일정)

#### 예시 2: 지연 해소 후 정상 진행

```dart
static const int delayDays = 0;
```

- 현재 날짜: 2025년 10월 15일
- 앱에서 표시되는 일정: 2025년 10월 15일의 일정
- 결과: 고린도전서 9-12장 (현재 일정)

## 적용되는 기능

일정 오프셋은 다음 기능들에 자동으로 적용됩니다:

1. **오늘의 말씀 (홈 화면)**
   - 오늘의 읽기 본문 표시
   - 유튜브 강의 링크

2. **일별 해설 이미지**
   - 일별 해설 이미지 URL 생성
   - 로컬/원격 이미지 경로

3. **주간 해설**
   - 해당 주차의 주간 해설 매칭

4. **캘린더 화면**
   - 특정 날짜의 일정 조회

## 주의사항

1. **앱 재빌드 필요**: 설정 변경 후 앱을 다시 빌드해야 적용됩니다.

2. **CSV 파일과의 관계**: 
   - `assets/data/url_list.csv`에는 원본 날짜가 저장되어 있습니다.
   - `delayDays` 설정으로 날짜를 조정하여 CSV에서 데이터를 찾습니다.
   - CSV 파일 자체를 수정할 필요는 없습니다.

3. **일관성 유지**: 
   - 모든 사용자가 같은 일정을 보려면 동일한 `delayDays` 값을 사용해야 합니다.
   - 앱 배포 시 올바른 값으로 설정했는지 확인하세요.

## 변경 이력

- 2025-10-15: 초기 오프셋 시스템 구현 (2주 지연 대응)

